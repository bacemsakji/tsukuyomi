[project]
name = "tsukuyomi"
version = "1.0.0"
description = "AI-powered Instagram DM bot with adaptive personality and scheduled messaging"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.33.1",
    "schedule>=1.2.2",
]
